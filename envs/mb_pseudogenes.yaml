channels:
  - nodefaults
  - conda-forge
  - bioconda
dependencies:
  - mafft
  - pip # TODO bioconda recipe for pytransaln so dependencies are explicitly specified
  - pip:
    - git+https://github.com/monagrland/pytransaln@c443efd
