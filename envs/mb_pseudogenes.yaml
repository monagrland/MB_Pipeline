channels:
  - nodefaults
  - conda-forge
  - bioconda
dependencies:
  - mafft # TODO explicit mafft version
  - pip # TODO bioconda recipe for pytransaln so dependencies are explicitly specified
  - pip:
    - pytransaln==0.2.0
